# Handoff — 05–06/09/2026 (אתר eliavafar.co.il)

## ⚠️ קודם כל — התיקייה זזה

הפרויקט הועבר ב-06/09/2026 ל-`D:\אפליקציות\_פרויקטים\אתר eliavafar\eliav-website`.
**הנתיב הישן `D:\אפליקציות\אתר eliavafar\...` אינו קיים יותר.**

אחרי ההעברה git סירב לגשת עם `dubious ownership` (התיקייה נרשמה כשייכת ל-`Guest`).
נפתר עם `git config --global --add safe.directory '<הנתיב החדש>'`. אם זה קורה בתיקייה אחרת שהועברה — אותה סיבה, אותו פתרון.

---

## מה נעשה בסשן הזה

### 1. אימות בדיקת האתר החיצונית (אליאב הריץ אותה במקום אחר)
אליאב הביא רשימת 15 ממצאים וביקש לדעת אם היא נכונה. **בדקתי כל טענה מול הקוד ומול פרודקשן.**
- **11 ממצאים אומתו** אחד לאחד.
- **2 שגויים:** (א) "ב-`llms.txt` יש שעות שישי" — אין שם שעות פתיחה בכלל, הסתירה שתוארה לא קיימת. (ב) "500+ פרויקטים מול 100+ בדף הבית" — אין "500+" בשום מקום ב-`projects.html`.
- **1 חצי-נכון:** "`/projects` דף מוסתר" — נכון שהוא לא בתפריט, **לא נכון שהוא ריק** (44KB, 4 קטגוריות + גלריה).

### 2. שלושה ממצאים חדשים שלא היו בבדיקה המקורית
1. 🔴 **כל עמוד מוגש בשתי כתובות עם 200.** נמדד חי: `/contact` → 200 וגם `/contact.html` → 200, בלי הפניה. במקביל ה-`canonical` מצביע על הנקייה, אבל **כל** הקישורים הפנימיים (78 ל-`contact.html`, 85 ל-`index.html`...) מצביעים על `.html`. זה הפער הטכני הגדול באתר.
2. ⛔ **`js/main.js` אינו הקובץ שרץ.** כל 14 העמודים טוענים `js/main.min.js`, אף אחד לא טוען את המקור. תיקון JS ב-`main.js` בלבד לא ישפיע על כלום.
3. **אין קובץ לוגו בריפו** (`find images -iname "*logo*"` → 0). לכן "תקן את הלוגו בסכמה" אינו תיקון של שורה אחת.

### 3. נכתב מפרט תיקונים מלא להעברה לסוכן חיצוני
📄 **`docs/FIX-SPEC-2026-09-05.md`** — 11 משימות לפי החזר-מול-מאמץ, כל אחת עם הממצא המאומת, מספרי שורות, התיקון המדויק ואיך לאמת. בראש המסמך 7 מלכודות (ISS-006/CLS, middleware ו-is-agentic 100, `main.min.js`, `check-all` לפני push).
אליאב מתכוון להעביר את זה לגרוק.

### 4. שתי תיקיות אתר — הוכרע
| | A — `_פרויקטים\אתר eliavafar\eliav-website` | B — `_פרויקטים\eliav-website` |
|---|---|---|
| קומיט אחרון | `6574265` · 04/09/2026 | `b5d3de0` · 28/05/2026 |
| קומיטים | **205** | 103 |
| סנכרון | `main`, 0 לפני origin | ישן |

**A היא החיה.** B מיותרת ובטוחה למחיקה — אומת בשלוש בדיקות: (1) ה-HEAD שלה מוכל במלואו בהיסטוריה של A; (2) `git status` נקי לגמרי; (3) השוואת קבצים רקורסיבית — **אפס קבצים קיימים ב-B ולא ב-A**.
**טרם נמחקה — ממתינה לאישור של אליאב.**

### 5. חיבור לגרוק — נבדק ונענה
Claude→גרוק אפשרי (API של xAI, תואם-OpenAI, דורש מפתח בתשלום) דרך סקריפט+Bash או שרת MCP.
גרוק→Claude **לא קיים** — אין ל-grok.com/X client של MCP ואין CLI רשמי.
**ההמלצה שניתנה: לא עכשיו** — מה שחסר לגרוק היה גישה לקוד, לא חוכמה; חיבור API רק יאוטמט את ההעתק-הדבק ולא יוסיף יכולת. שווה רק אם רוצים לולאת ביקורת קבועה לפני כל דחיפה.

---

## פערים פתוחים

| # | מה | אצל מי |
|---|-----|--------|
| 1 | **הגנת ספאם ב-`/api/notify-lead` (ISS-023)** — ⚠️ **בדקתי: honeypot ו-rate-limit-לפי-IP כבר קיימים ומחווטים** (`lib/rate-limit.js`, קומיטים `698c86a`/`d3281d9`/`8184e12`). מה שבאמת חסר: חסימת דפוס `555-01xx` ו/או ולידציה שהטלפון ישראלי. **ISS-023 מתאר מצב לא מעודכן — לתקן את הרישום.** התחלתי ונעצרתי לבקשת אליאב | אני |
| 2 | **11 המשימות ב-`docs/FIX-SPEC-2026-09-05.md`** — במיוחד #1 (בורות חלחול בטופס, 2 דק') ו-#2 (כתובות כפולות) | אליאב מעביר לגרוק |
| 3 | **מחיקת `_פרויקטים\eliav-website`** — אומת כבטוח, ממתין למילה אחת | אליאב |
| 4 | 4 תיקיות עדיין לא הועברו ל-`_פרויקטים` (נעולות ע"י תהליכים רצים): ניהול ומעקב עסק · ניהול כספים ביתי · מערכת תמחור · מיון תמונות. ראה `D:\אפליקציות\_ממתין-להעברה.md` | אליאב |
| 5 | **GA4 בלי Key Events + GSC לא מקושר** — כל תיקון שיבוצע לא יהיה ניתן למדידה. 10 דק', ולפני גל התיקונים ולא אחריו | אליאב |
| 6 | 4 טענות דורשות בדיקה בדפדפן חי: חיתוך hero ב-RTL מובייל · וואטסאפ צף חוסם CTA · המחשבון לא נטען (⚠️ ה-iframe **כן** קיים, `drainage-pits.html:420`, חשוד: `loading="lazy"` או גובה 560px קבוע) · מיון הבלוג | אני |
| 7 | מיזוג 2 פרופילי GBP + תיקון `http://`→`https://`, ואחריו `aggregateRating` ל-26/4.8 | אליאב, ואז אני |
| 8 | `OPEN_PAGE_RANK_KEY` שבור — חידוש + GitHub Secrets | אליאב |
| 9 | LCP 3.8s מובייל. ⛔ **הטמעת CSS קריטי אסורה — גרמה ל-CLS 1.0 ב-ISS-006** | אני, בהחלטתו |
| 10 | `--browserUrl` ל-Chrome האמיתי (Google חוסם התחברות בדפדפן שנשלט ב-CDP) | החלטה של אליאב |

## איפה עצרנו
אליאב שאל אם אפשר לחבר אותי לגרוק דו-כיוונית. עניתי (ראה סעיף 5) והמלצתי לא עכשיו.
**הצעד הבא הכי טבעי: פער #1** — קצר, שלי, ודורש קודם תיקון של הרישום ב-ISS-023 שכבר לא משקף את המצב בקוד.
