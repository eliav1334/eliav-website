
---

## עדכון 08/09/2026 — פער הספאם נסגר (בוצע ע"י גרוק, אומת על ידי)

**השורש שהתגלה:** הלידים הספאמיים **לא עברו דרך השרת שלנו**. הטופס נשא `action="https://formsubmit.co/eliav1334@gmail.com"` בעשרות עמודים, ובוטים שלחו POST ישירות ל-FormSubmit בעקיפת האתר. ⛔ **לכן כל ההגנות שב-`/api/notify-lead` היו חסרות משמעות** — הבוט מעולם לא הגיע אליהן.

**מה נעשה, ומה אומת חי בפרודקשן:**

| בדיקה | תוצאה |
|-------|--------|
| POST בלי Origin/Referer | **403** ✅ |
| POST + Origin תקין + טלפון `2025550145` | **`filtered:true`, ללא מייל** ✅ |
| `formsubmit` בקבצי HTML | **0** — הוסר ✅ |
| `_formts` ב-`main.js` וגם ב-`main.min.js` | 7 ו-1, שניהם נבנו באותה שנייה ✅ |
| `tests/spam-filter.test.js` | קיימת, 5 חבילות ✅ |
| ולידציית טלפון ישראלי | `/^05\d{7,8}$/` ✅ |
| `PER_IP` | הורד מ-5 ל-3 |

**שלושה סיכונים שנותרו למעקב:**

1. 🟠 **`checkOrigin` הפך החלטה מודעת קודמת.** הקוד המקורי **בכוונה** אישר בקשות בלי Origin/Referer, עם הערה מפורשת: תוספי פרטיות מסירים אותן, וליד אבוד עולה כסף אמיתי בעוד שמייל ספאם עולה עשר שניות. עכשיו זה 403. הסיכון בפועל **נמוך** (אין באתר `Referrer-Policy: no-referrer`, ודפדפנים שולחים Referer באותו דומיין), אבל זה **כשל שקט** — ליד חסום לא מייצר מייל ולא התראה. **מומלץ: מונה/לוג לחסימות 403 כדי לראות אם המספר קופץ.**
2. **`_formts` מגיע מהדפדפן** — בוט יכול לזייף חותמת זמן ולעבור. שכבה, לא חומה.
3. 🟡 **`formsubmit` עדיין קיים ב-`js/main.js` וב-`js/main.min.js`** (מסלול גיבוי). `main.min.js` הוא קובץ ציבורי, כלומר כתובת המייל עדיין חשופה לסריקה — חשיפה קטנה יותר מטופס HTML, אך לא אפס. שווה להסיר: Brevo תקין ומאומת, הגיבוי מיותר.

**משימה ידנית לאליאב:** למחוק מהמייל את ליד הבדיקה בשם "בדיקת מערכת אתר".

**סטטוס ISS-023: ראוי לסגירה**, עם הערה על סעיף 1 כפער לניטור.

**נותר פתוח מ-`docs/FIX-SPEC-2026-09-05.md`:**
- משימה 6 — `lastmod`: 29 ערכים נצרבו ידנית ל-`2026-08-24`. תאריך אחיד ולא-אמיתי על כל האתר הוא אות שלילי. עדיין צריך `scripts/gen-sitemap.mjs` שמושך תאריכים מ-`git log`.
- משימה 8 — לוגו: עדיין `hero-new-1.webp`. אין קובץ לוגו בריפו.
- משימה 11 — באנר cookies: לא בוצע.
- 19 קישורי `.html` פנימיים למאמרי בלוג — מחזירים 301 תקין, רק קפיצה מיותרת.

**רגרסיות שכדאי לזכור:** PR#42 גרם ל-4 רגרסיות שדרשו PR#43 ו-PR#44 (רוחב FAQ, גריד השירותים, וי במחשבון, ולידציית טופס). גריד השירותים הוא בדיוק הסיכון שסומן במפרט במשימה 3 — כרטיס רביעי בגריד של שלוש עמודות.
