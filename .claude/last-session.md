# Session Handoff — 19/07/2026 (eliavafar)

## Status
בריא ✅. `npm run check-all` → meta 35 · visual 34 · site (16 ראשיים + 17 בלוג) — הכל תקין. git נקי, מסונכרן עם origin/main. **9 קומיטים.**

⚠️ **חדש: `npm run check-all`** = `check-meta` + `check-visual` + `check-site`. להריץ את זה, לא כל אחד לחוד.

---

## ✅ נעשה ונדחף

### 1. דף חדרה (`92f796c`)
`blog/drilling-hadera.html` — 1,836 מילים. חדרה הוזכרה **26 פעם** באתר בלי דף ייעודי. מיקוד ב**בור חלחול בחדרה**, קרבת אליכין כבידול, כרטיס נפרד לכל יישוב, 7 שאלות מקומיות. + `blog.html`, `vercel.json` (rewrite — בלעדיו ה-canonical 404), `sitemap.xml`.

### 2. 🔴 תיקון מקצועי — פסיקת אליאב על נוזלי קידוח (`bfd24cb`, `210a55b`)
**כתבתי באתר מידע מקצועי שגוי.** אליאב תפס. ⇒ **[[eliavafar_drilling_fluid_rules]] בזיכרון — לקרוא לפני כל תוכן מקצועי.**
- **חלחול = ספיגה = ניקוז** (מילים נרדפות) ⇒ **אף פעם לא בנטונייט** (סותם את הנקבוביות). מרטיבים חול, ולפעמים זה פשוט לא תופס.
- **החדרה + בארות ⇒ פולימר** · **כלונסאות/דיפון ⇒ בנטונייט**
- **בדיקות קרקע כן** (קידוח ניסיון + SPT), **תכנון לא**
- העפר **לא תמיד מפונה** — לפעמים מפוזר במקום
- תוקן ב-**11 קבצים** כולל **`llms-full.txt`** — הקובץ שמזין מנועי AI **הזין להם מידע שגוי**.

### 3. מחירים (`dfd518a`, `8246a1f`)
- שורת "בורות חלחול" בטבלאות **מחירי בנטונייט** תומחרה **למ״א** ⇒ בור 6 מ׳ יצא 7,200-10,800₪ בעוד `drainage-pit-home` אומר 5,000-8,000 לבור שלם. **סתירה פנימית בדף שמדורג.** הוחלף ב-3 מדרגות לבור שלם מהמחירון הקיים; כל שורה מציינת בסיס (`למ״א` / `לבור`).
- **סעיף חדש** `bentonite-guide#compare-quotes` — "למה יש הצעה של 100₪ ושל 1,400₪", משל **תפוח לתפוח**, ו-6 שאלות לקבלן. `bentonite-drilling-cost` **מקשר** אליו (עותק אחד, בלי קניבליזציה).

### 4. ניטור (`9815ee1`)
הדוח אמר "28 דפים תקינים" בעוד 34 קבצים חיים. **4 דפים לא נוטרו** — `drainage-pit-home`, `drainage-calculator`, `review`, `review-qr`. שבירה בהם לא הייתה מדווחת.

### 5. שלושה באגי מובייל + שומר (`549ad7a`) — ISS-014
- **מילים נדבקו:** `.why-card p br{display:none}` במובייל, אבל ב-HTML אין רווח סביב ה-`<br/>` ⇒ `לתוצאההטובה`. 6 כרטיסים.
- **משקל גופן שקרי:** H1 ב-`font-weight:800` על Rubik, אבל הוזמנו 400-700 בלבד. **אומת: רוחב 700 ו-800 זהה (526px)** ⇒ רונדר ב-700 בשקט. 800 נוסף ב-31 דפים.
- **כפתור הנגישות כיסה את "הצהרת נגישות"** — `footer padding-bottom` 80px מול כפתור שמגיע 205px מעל התחתית. → 220px.
- 🛡️ **`scripts/check-visual.mjs`** (`npm run check-visual`) — כלל אחד לכל באג, סטטי.

### 6. ניקוי (`1512456`)
10 דפי `logos-*.html` + `logo-options/` נמחקו (אליאב: "לא אהבתי, ניצור חדשים"). שחזור מ-`92f796c`.

---

## 🔻 שתי טעויות שלי הסשן — לקחים

1. **אבחנתי "הגופנים לא נטענים"** (`document.fonts` = 0 loaded) והייתי צעד מלעשות אירוח-עצמי מיותר. **מדידה חוזרת אחרי `fonts.ready` + השהיה: הם כן נטענים.** קראתי מוקדם מדי תחת חנק.
2. **קודקס תפס טעות לוגית:** *"LCP של טקסט נרשם כשהטקסט מצויר לראשונה, לא כשהגופן הסופי מגיע."* ⇒ תיקון הגופנים לא היה משפר LCP בכלל.
3. **הגרסה הראשונה של `check-visual` לא תפסה כלום** — הרגקס עצר ב-`</div>` של האייקון. נתפס רק כי החזרתי את הבאגים ובדקתי שהיא נכשלת.

---

## 📊 ביצועים — נחקר, לא נדרשת פעולה

דוח שבועי: LCP 6.1s · CLS 0.101. **לא שוחזר.** תחת חנק זהה (מעבד ×4, Slow 4G, 390px):
| | דוח | קר | חם |
|---|---|---|---|
| LCP | 6.1s | **2.08s** | **0.78s** |
| CLS | 0.101 | **0.00** | **0.00** |

נתיב קריטי = `HTML(669ms) → style.min.css(1,776ms)`. **הגופנים לא עליו.** כרום מדווח חיסכון מחוסמי-רינדור = **0ms**. Lighthouse משתמש בחנק *מדומה* (פסימי יותר) — פער ידוע.
⛔ **לא לנקות CSS אוטומטית** — בדקתי, 58% "לא בשימוש" אבל הבדיקה סימנה `:hover`, `header.scrolled`, `.header-inner` כמיותרים. ניקוי אוטומטי ישבור את האתר.

---

## 🔴 פתוח

### דורש אליאב
1. **מיזוג פרופילי Google** — הכי משתלם. הביקורות **מפוצלות בין שני פרופילים** ⇒ גוגל רואה שני עסקים חלשים. מסלול: [support.google.com/business/gethelp](https://support.google.com/business/gethelp) → `Merge duplicate profiles` → Email. **חובה בדפדפן הרגיל שלו** (גוגל חוסמת התחברות בדפדפן אוטומטי). נוסח מוכן בסשן. ⚠️ הכתובות שונות (שקד / שקד 11) — עלול להידחות.
2. **לוגו** — חוסם שובר Google Ads ₪1,500. הקודמים נמחקו.
3. **תעריף היטל הטמנה 2026** — \*6911.
4. **ניקוי Brevo** — 5 מיילי `test-audit-noop` + 4 אנשי קשר בדיקה.
5. **לאשר: כמות ברזל** — הנחתי 0.5% (ת״י 940) = 39 ק״ג/קוב. אם רחוק מהמציאות, ההצלבה משתנה.

### אני יכול — ממתין ל"כן"
6. **מנוע ביקורות** — המנוף הכי גדול (0 דפים במיקום 1).
7. **עקביות שם העסק** — B144 / מידרג / דפי זהב. גם עוזר למיזוג להתקבל.
8. **וידאו — אפס באתר.** בדקתי: אין ולו סרטון אחד. למתחרים יש, וזו הוכחת-ביצוע חזקה.
9. **מקרי בוחן** — יש תמונות, אין סיפור (עומק/קוטר/גישה).
10. **דפי ערים** — עמק חפר, כפר יונה. (נתניה, הוד השרון, חדרה ✅)
11. **Bing** Webmaster + Places.

---

## ⚠️ גוצ'ות
- **`?cb=$RANDOM` חובה** — גם באימות **מקומי**. קאש דפדפן הגיש HTML ישן עם `?v=` ישן וגרם ל"התיקון נכשל" שקרי.
- **`document.fonts` / מדדי ביצועים** — לקרוא רק אחרי `await document.fonts.ready` + השהיה.
- **מדידה לא-מחונקת ≠ Lighthouse מחונק.** לשחזר תנאים לפני שמסיקים.
- **בדיקה שעוברת חסרת ערך** עד שמוכיחים שהיא נכשלת על הבאג. תמיד להחזיר את הבאג.
- **דפדפן MCP ננעל** — playwright ו-chrome-devtools לא יכולים לרוץ יחד. `browser_close` + מחיקת `Singleton*` מהפרופיל.
- **`npm run stamp`** אחרי כל שינוי css/js.
- **נתיב עברי ב-Node** — `fileURLToPath`, לא `URL.pathname` (מחזיר percent-encoded).
- ⛔ **אסור critical-CSS inline** (ISS-006, CLS 1.0).
- **גרשיים `"` בתוך `content="…"` שוברים meta** — `״` במקום.

## Next Session Prompt
פרויקט: eliavafar.co.il (HTML/CSS/JS סטטי, Vercel auto-deploy מ-main). סשן 19/07 סגר: דף חדרה, **תיקון מקצועי רוחבי על נוזלי קידוח שאליאב תפס** (11 קבצים + llms-full.txt), מחירי בורות חלחול, סעיף "תפוח לתפוח", 3 באגי מובייל + `check-visual.mjs`, ו-4 דפים שלא נוטרו. פתוח: **מיזוג פרופילי Google (אליאב)**, לוגו, מנוע ביקורות, וידאו, עקביות שם. **קרא `.claude/last-session.md` + זיכרון `eliavafar_drilling_fluid_rules` (חובה לפני תוכן מקצועי) + `eliavafar_drilling_cost_structure` (⛔ לא לפרסם).**
