# Session Handoff — 13/07/2026 (eliavafar)

## Status
בריא ✅. `npm run check-site` → 28 דפים, SSL, לידים+Brevo, schema — הכל תקין. git נקי, הכל נדחף. **11 קומיטים הסשן.**

⚠️ **הפרוטוקול הקבוע נשמר בזיכרון:** `eliavafar_site_health_protocol.md` — כשאליאב אומר "בדוק תקינות"/"תקפיץ את האתר" ⇒ **לבצע, לא להציע.** לשאול רק על מה שפיזית רק הוא יכול, עם קישור + הוראות.

---

## ✅ נעשה ונדחף

### 1. המחשבון עבר לאן שהתנועה נמצאת (`0b9b340`)
GSC יוני חשף: **76% מהקליקים (72/95) מ-3 מאמרי מחיר בבלוג.** `drainage-pits-pricing` לבדו = 30 קליקים על "קידוח בור חלחול מחיר" — **והמחשבון שעונה בדיוק על זה לא היה שם.** הוטמע אחרי טבלת המחירים + קישור מ-`drainage-pits-guide`.
**+ הגנת פופאפ:** שני הפופאפים (טיימר 45ש', גלילת 40%) נחתו בדיוק על המחשבון. `window.__aaPopupBlocked()` — המחשבון שולח `pcEngaged` ב-postMessage; הפופאפים נדחים כשהוא על המסך ומושתקים ברגע שמתחילים לחשב.

### 2. אבטחה — 4 פערים (`d3281d9`, `f9a4d3e`)
- 🔴 **אין הגבלת קצב** — אומת חי: 5 POST רצופים ל-`/api/notify-lead`, כולם 200. כל בקשה = מייל Brevo אמיתי ⇒ **דיוור חינם לתוקף.** נוסף `lib/rate-limit.js` (5/10דק'/IP + 40/מופע).
- 🔴 **הזרקה למייל שלנו** — `phone` נוקה מרווחים אך **לא הוברח**, והושתל ב-`href="..."`. תוקן בשורש: ספרות בלבד.
- **`checkOrigin()`** — CORS מגן על הדפדפן, **לא על השרת**. POST חוצה-מקור הגיע לפונקציה. עכשיו 403. ⚠️ **מכוון: מרשה כשאין Origin/Referer** (תוספי פרטיות) — ליד אבוד > מייל ספאם.
- honeypot חסר בפופאפ-הטיימר. נוסף.
- ✅ **אליאב הדליק Vercel WAF** (חינם, Hobby) — אומת: בקשה 21 → **429**. יושב לפני הפונקציה.

### 3. 🔴 תקלה שקטה — רשימת התפוצה לא נבנתה חודשים
`subscribe.js` שלח ל-Brevo **בלי מזהה** כשאין אימייל. Brevo דורש `email` או `ext_id` ⇒ **כל ליד עם טלפון-בלבד (=הרוב מאז שהאימייל הפך לא-חובה) נדחה בשקט.** הפניות עצמן הגיעו (נתיב נפרד) — לא אבדו לקוחות. תוקן: `ext_id` + `SMS`.

### 4. ביצועים (`2b8fd28`)
מדידה אמיתית: **LCP = טקסט, 96% עיכוב-רינדור** (TTFB כבר 62ms).
- `accessibility.css` חסם **29 דפים** — החוליה האיטית בשרשרת (1215ms). נדחה (media=print). **כל כלל בו `position:fixed` ⇒ CLS נשאר 0.**
- `accessibility.js` **בלי `defer` ב-27/29 דפים**. תוקן + `fetchpriority=low`.
- 4 דפי שירות עשו `preload fetchpriority=high` לתמונת גלריה **שאינה ה-LCP** ומסומנת lazy. ב-earthworks היא **לא בדף בכלל**. הוסרו.
- קאש css/js: שעה → **שנה immutable**. הוצרך `scripts/stamp-assets.mjs` (`npm run stamp`) — `accessibility.css/js` לא נשאו `?v=` כלל ⇒ קאש-שנה היה נועל גולשים לנצח.

### 5. SEO — קניבליזציה (`473f686`)
6,560 חשיפות, **0 דפים במיקום 1**. הכותרות תקינות — **הדירוג** הבעיה.
- זוגות "מדריך+מחיר" התחרו **בלי אף קישור** (0 בכל 4 הכיוונים). `bentonite-guide` אפילו הכריז "מחירון 2026". שונה ל"איך התהליך עובד" + קושרו הדדית.
- `foundation-piles-guide` — **התוכן כבר ענה "כמה עולות"** (800-2,500₪) אך הכותרת הסתירה ⇒ בלתי-נראה לתבנית שמביאה 76%. שונה.
- מחירים אמיתיים נכנסו לתיאורי meta (נשלפו מהמאמרים).
- 🔴 **באג שיצרתי:** `ס"מ` בתוך `content="…"` **שובר את התגית.** תוקן לגרשיים עבריים `״`. נוסף `npm run check-meta`.

### 6. מאמר פסולת (`4c1446d`→`9d3813e`)
- **תיקון חוק פסולת בניין, 9/6/2026** — אושר בוועדת הפנים. מנוסח מדויק: *טרם חוק*. אף מתחרה לא כתב.
- **ההגדרה הרשמית כוללת "ערֵמות אדמה"** ⇒ **עודפי עפר = פסולת בניין.** נופל בול על עסקי העפר שלו. שאלה ראשונה ב-FAQ.
- **אגרגטים ממוחזרים מוזילים בנייה 5%-10%** (משהב"ס) — הפך הוצאה להזדמנות.
- **היטל הטמנה: מתחיל בכ-85₪/טון** — נתון של אליאב מהשטח. ⚠️ **מנוסח כ"סדר גודל, לא מחיר להסתמך עליו"** — הוא לא נותן ללקוח את המחיר שהוא משלם. ההסתייגות נוסעת עם המספר לכל מקום (גוף/FAQ/סכמה/תיאור-גוגל).

### 7. 🔴 ISS-010 — כל הפנימיים היו חשופים (`9f1c2e4`)
**Vercel מגישה את כל ה-repo.** אומת: `docs/ISSUES-LOG.md` (=דוח האבטחה שלנו!), `CLAUDE.md`, `scripts/`, `.github/` — **כולם 200.**
🔴 **199MB תמונות גולמיות נפרסו** — תמונה בודדת יורדת ב-**9.9MB**. **`.gitignore` לא מסיר ניהול מקובץ שכבר מנוהל** (סוכן הביצועים דיווח בטעות שהן לא נפרסות; `git ls-files` הפריך).
תוקן: `.vercelignore` + `git rm --cached`. **אין credential שדלף** (רק שמות משתנים).

---

## 🔴 פתוח

### דורש אליאב
1. **מיזוג פרופילי Google** — עצר באמצע. הכפיל (שקד) צומח +52%, הראשי (שקד 11) צונח **-51%**. קניבליזציה פעילה.
2. **תעריף היטל הטמנה 2026 מדויק** — לא מפורסם בשום מקור נגיש (gov.il חוסם אוטומציה). מוקד המשרד להגנת הסביבה: **\*6911**. (85₪ כבר במאמר כ"סדר גודל".)
3. **לוגו** — חוסם שובר Google Ads של ₪1,500.
4. **ניקוי:** 5 מיילים `test-audit-noop` + 4 אנשי קשר "בדיקה למחיקה" ב-Brevo.

### אני יכול לבצע — ממתין ל"כן"
5. **מנוע ביקורות** — המנוף הכי גדול (0 דפים במיקום 1).
6. **גלריה** — 29 תמונות נקיות ב-`E:\ריכוז כל התמונות\_gallery_pick\`.
7. **רילים** מסרטוני העבודה.
8. `logos.html` + `logo-options/` עדיין בפרוד — לנקות אחרי החלטת לוגו (#3).

### לא-אומת
9. **`lib/rate-limit.js` עדיין חשוף** (200). הוצאתו תשבור את ה-API (`require` בזמן ריצה). ניסיתי `api/_lib/` על ענף — Vercel לא חשפה preview. **החלטה: לא לדחוף שינוי לא-מאומת לצינור לידים.** סיכון נמוך (סף מתגלה אמפירית; WAF לפניו).

---

## ⚠️ גוצ'ות (חדשות הסשן)
- **קאש CDN מסתיר דיפלוי** — לאמת HTML בפרוד **תמיד** עם `?cb=$RANDOM`. בלי זה מקבלים תוכן ישן ונראה ככישלון. גם כותרות `Cache-Control` נשמרות.
- **Vercel פורסת פונקציות אחרי סטטיים** (~20ש') — לאמת שינוי ב-`api/` מול ה-endpoint, לא מול קובץ סטטי.
- **`.gitignore` ≠ לא-מנוהל** — לאמת ב-`git ls-files`, לא להניח.
- **גרשיים `"` בתוך `content="…"` שוברים meta** — להשתמש ב-`״`. `npm run check-meta` תופס.
- **`npm run stamp` אחרי כל שינוי css/js** — קאש שנה immutable.
- **ריצת GitHub Actions "cancelled" עם 0 שלבים** = GitHub לא הקצה מכונה = **אזעקת שווא**, לא באג.
- ⛔ **אסור critical-CSS inline** (ISS-006, CLS 1.0). CLS כרגע 0.
- ⛔ **לא להמציא מספר רגולטורי.** אליאב שלח **טבלת תמלוגי רמ"י** בחושבו שזה היטל הטמנה — זה תשלום של מפעילי מטמנות על **קרקע**. פרסום כ"היטל הטמנה" = מידע שגוי ללקוחותיו.
- **סכמת ביקורות/FAQ לא מרנדרת** לעסקים כאלה (גוגל חסמה 2019/2023) — לא להשקיע שם.

## Next Session Prompt
פרויקט: eliavafar.co.il (HTML/CSS/JS, Vercel auto-deploy מ-main). סשן 13/07 סגר: ביקורת אבטחה (4 פערים + Vercel WAF שאליאב הדליק), ביצועים (accessibility.css חסם 29 דפים), SEO (קניבליזציה בין מאמרי הכסף), מחשבון הוטמע במאמר שמביא 30 קליקים, מאמר פסולת שודרג (חוק חדש + היטל 85₪), ו-ISS-010: 199MB + כל התיעוד הפנימי היו חשופים בפרוד — נסגר. פתוח: מנוע ביקורות (המנוף הכי גדול), גלריה, מיזוג פרופילי Google, לוגו. **קרא `.claude/last-session.md` + זיכרון `eliavafar_site_health_protocol.md` (לבצע, לא להציע).**
