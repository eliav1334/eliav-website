name: "📊 בדיקה שבועית - eliavafar.co.il"

on:
  schedule:
    # כל יום ראשון ב-08:00 שעון ישראל (06:00 UTC)
    - cron: '0 6 * * 0'
  workflow_dispatch: # אפשרות להרצה ידנית מ-GitHub

jobs:
  weekly-check:
    runs-on: ubuntu-latest
    permissions:
      issues: write

    steps:
      - name: "🔍 בדיקת דפי האתר"
        id: pages
        run: |
          SITE="https://eliavafar.co.il"
          PAGES=("/" "/bentonite-drilling" "/earthworks" "/drainage-pits" "/equipment-rental" "/contact" "/thanks")
          NAMES=("דף ראשי" "קידוחי בנטונייט" "עבודות עפר" "בורות חלחול" "השכרת ציוד" "צור קשר" "דף תודה")

          RESULTS=""
          ALL_OK=true

          for i in "${!PAGES[@]}"; do
            URL="${SITE}${PAGES[$i]}"
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$URL" 2>/dev/null)

            if [ "$STATUS" = "200" ]; then
              RESULTS="${RESULTS}| ${NAMES[$i]} | ${PAGES[$i]} | ✅ ${STATUS} |\n"
            else
              RESULTS="${RESULTS}| ${NAMES[$i]} | ${PAGES[$i]} | ❌ ${STATUS} |\n"
              ALL_OK=false
            fi
          done

          echo "results<<EOF" >> $GITHUB_OUTPUT
          echo -e "$RESULTS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          if [ "$ALL_OK" = true ]; then
            echo "status=✅ כל הדפים תקינים" >> $GITHUB_OUTPUT
          else
            echo "status=❌ יש דפים עם בעיות!" >> $GITHUB_OUTPUT
          fi

      - name: "🔒 בדיקת SSL"
        id: ssl
        run: |
          EXPIRY=$(echo | openssl s_client -servername eliavafar.co.il -connect eliavafar.co.il:443 2>/dev/null | openssl x509 -noout -enddate 2>/dev/null | cut -d= -f2)

          if [ -n "$EXPIRY" ]; then
            EXPIRY_EPOCH=$(date -d "$EXPIRY" +%s 2>/dev/null)
            NOW_EPOCH=$(date +%s)
            DAYS_LEFT=$(( (EXPIRY_EPOCH - NOW_EPOCH) / 86400 ))

            if [ "$DAYS_LEFT" -gt 30 ]; then
              echo "status=✅ SSL תקף - נותרו ${DAYS_LEFT} ימים (עד ${EXPIRY})" >> $GITHUB_OUTPUT
            elif [ "$DAYS_LEFT" -gt 0 ]; then
              echo "status=⚠️ SSL פג בקרוב! נותרו ${DAYS_LEFT} ימים בלבד!" >> $GITHUB_OUTPUT
            else
              echo "status=❌ SSL פג תוקף!" >> $GITHUB_OUTPUT
            fi
          else
            echo "status=⚠️ לא ניתן לבדוק SSL" >> $GITHUB_OUTPUT
          fi

      - name: "⚡ בדיקת מהירות (PageSpeed)"
        id: speed
        run: |
          API="https://www.googleapis.com/pagespeedonline/v5/runPagespeed"
          URL="https://eliavafar.co.il"

          # בדיקת מובייל (timeout 60 שניות - PageSpeed לוקח זמן)
          echo "Checking mobile speed..."
          MOBILE=$(curl -s --max-time 120 "${API}?url=${URL}&category=performance&strategy=mobile" 2>/dev/null)
          MOBILE_SCORE=$(echo "$MOBILE" | python3 -c "
          import sys, json
          try:
              d = json.load(sys.stdin)
              print(int(d['lighthouseResult']['categories']['performance']['score']*100))
          except:
              print('N/A')
          " 2>/dev/null)

          # בדיקת דסקטופ
          echo "Checking desktop speed..."
          DESKTOP=$(curl -s --max-time 120 "${API}?url=${URL}&category=performance&strategy=desktop" 2>/dev/null)
          DESKTOP_SCORE=$(echo "$DESKTOP" | python3 -c "
          import sys, json
          try:
              d = json.load(sys.stdin)
              print(int(d['lighthouseResult']['categories']['performance']['score']*100))
          except:
              print('N/A')
          " 2>/dev/null)

          # LCP מובייל
          MOBILE_LCP=$(echo "$MOBILE" | python3 -c "
          import sys, json
          try:
              d = json.load(sys.stdin)
              print(d['lighthouseResult']['audits']['largest-contentful-paint']['displayValue'])
          except:
              print('N/A')
          " 2>/dev/null)

          echo "Mobile: ${MOBILE_SCORE}, Desktop: ${DESKTOP_SCORE}, LCP: ${MOBILE_LCP}"

          echo "mobile=${MOBILE_SCORE}" >> $GITHUB_OUTPUT
          echo "desktop=${DESKTOP_SCORE}" >> $GITHUB_OUTPUT
          echo "lcp=${MOBILE_LCP}" >> $GITHUB_OUTPUT

          # אייקון לפי ציון
          if [ "$MOBILE_SCORE" != "N/A" ] && [ "$MOBILE_SCORE" -ge 90 ]; then
            echo "mobile_icon=🟢" >> $GITHUB_OUTPUT
          elif [ "$MOBILE_SCORE" != "N/A" ] && [ "$MOBILE_SCORE" -ge 50 ]; then
            echo "mobile_icon=🟡" >> $GITHUB_OUTPUT
          else
            echo "mobile_icon=🔴" >> $GITHUB_OUTPUT
          fi

          if [ "$DESKTOP_SCORE" != "N/A" ] && [ "$DESKTOP_SCORE" -ge 90 ]; then
            echo "desktop_icon=🟢" >> $GITHUB_OUTPUT
          elif [ "$DESKTOP_SCORE" != "N/A" ] && [ "$DESKTOP_SCORE" -ge 50 ]; then
            echo "desktop_icon=🟡" >> $GITHUB_OUTPUT
          else
            echo "desktop_icon=🔴" >> $GITHUB_OUTPUT
          fi

      - name: "🔗 בדיקת לינקים שבורים"
        id: links
        run: |
          SITE="https://eliavafar.co.il"
          BROKEN=""
          BROKEN_COUNT=0

          # בדיקת לינקים חיצוניים חשובים
          LINKS=(
            "https://wa.me/972529556123"
            "https://fonts.googleapis.com/css2?family=Heebo"
            "https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"
          )
          LINK_NAMES=(
            "WhatsApp"
            "Google Fonts"
            "Swiper CDN"
          )

          for i in "${!LINKS[@]}"; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 -L "${LINKS[$i]}" 2>/dev/null)
            if [ "$STATUS" != "200" ] && [ "$STATUS" != "301" ] && [ "$STATUS" != "302" ]; then
              BROKEN="${BROKEN}| ${LINK_NAMES[$i]} | ${LINKS[$i]} | ❌ ${STATUS} |\n"
              BROKEN_COUNT=$((BROKEN_COUNT + 1))
            fi
          done

          if [ "$BROKEN_COUNT" -eq 0 ]; then
            echo "status=✅ כל הלינקים החיצוניים תקינים" >> $GITHUB_OUTPUT
            echo "details=" >> $GITHUB_OUTPUT
          else
            echo "status=❌ נמצאו ${BROKEN_COUNT} לינקים שבורים" >> $GITHUB_OUTPUT
            echo "details<<EOF" >> $GITHUB_OUTPUT
            echo -e "$BROKEN" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: "📋 יצירת דוח Issue"
        uses: actions/github-script@v7
        with:
          script: |
            const today = new Date().toLocaleDateString('he-IL', {
              year: 'numeric', month: 'long', day: 'numeric',
              timeZone: 'Asia/Jerusalem'
            });

            const pagesStatus = `${{ steps.pages.outputs.status }}`;
            const sslStatus = `${{ steps.ssl.outputs.status }}`;
            const mobileScore = `${{ steps.speed.outputs.mobile }}`;
            const desktopScore = `${{ steps.speed.outputs.desktop }}`;
            const mobileLcp = `${{ steps.speed.outputs.lcp }}`;
            const mobileIcon = `${{ steps.speed.outputs.mobile_icon }}`;
            const desktopIcon = `${{ steps.speed.outputs.desktop_icon }}`;
            const linksStatus = `${{ steps.links.outputs.status }}`;
            const linksDetails = `${{ steps.links.outputs.details }}`;

            const body = `## 📊 דוח שבועי - eliavafar.co.il
            **תאריך:** ${today}

            ---

            ### 🌐 סטטוס דפים
            ${pagesStatus}

            | דף | נתיב | סטטוס |
            |----|------|-------|
            ${{ steps.pages.outputs.results }}

            ### 🔒 אבטחה (SSL)
            ${sslStatus}

            ### ⚡ מהירות
            | מדד | ציון |
            |-----|------|
            | מובייל | ${mobileIcon} ${mobileScore}/100 |
            | דסקטופ | ${desktopIcon} ${desktopScore}/100 |
            | LCP (מובייל) | ${mobileLcp} |

            ### 🔗 לינקים חיצוניים
            ${linksStatus}
            ${linksDetails ? '\n| שם | URL | סטטוס |\n|----|-----|-------|\n' + linksDetails : ''}

            ---

            ### 📋 תזכורת - לבדוק ידנית השבוע:

            - [ ] **GA4** (analytics.google.com) - כמות מבקרים, המרות, דפים פופולריים
            - [ ] **Clarity** (clarity.microsoft.com) - הקלטות משתמשים, מפות חום
            - [ ] **Google Business** (business.google.com) - ביקורות חדשות, לענות על ביקורות
            - [ ] **Search Console** (search.google.com/search-console) - שגיאות, מילות חיפוש

            ---
            *דוח אוטומטי שנוצר ע"י GitHub Actions*`;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `📊 דוח שבועי - ${today}`,
              body: body,
              labels: ['weekly-report']
            });
