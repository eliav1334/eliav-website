# ביקורת SEO — eliavafar.co.il · 08/09/2026

**היקף:** 19 עמודי שורש + 17 מאמרי בלוג = 36 עמודים. כל ממצא כאן נמדד בפקודה, לא הוערך.

---

## ✅ מה שנמצא תקין (כולל תיקון הערכות קודמות שלי)

| נבדק | תוצאה |
|------|--------|
| כפילות `.html` מול כתובת נקייה | **נסגרה.** `curl` על `/bentonite-drilling.html` → **301** → `/bentonite-drilling`. ההערכה הקודמת ("שתיהן 200") אינה תקפה יותר |
| `"logo"` ו-`openingHoursSpecification` בסכמה | **קיימים** בפועל. ההערכה "חסרים" הייתה שגויה |
| `aria-expanded` ב-FAQ | **7 מופעים** ב-`index.html`, 7 ב-`bentonite-drilling`, 3 ב-`drainage-pits`. ההערכה "אפס" הייתה שגויה |
| canonical | קיים בכל 36 העמודים, ייחודי. הכפילות היחידה (`/earthworks` פעמיים) היא מ-`demolition.html` שהוא `noindex` — תקין |
| עמודי `noindex` בסייטמאפ | **אפס.** 404/card/thanks/review/review-qr/demolition כולם מחוץ לסייטמאפ ✅ |
| `robots.txt` | מצוין — כולל הרשאות מפורשות ל-GPTBot/Claude-Web/Perplexity + `llms.txt`, וחסימת AhrefsBot/SemrushBot |
| Schema.org | עשיר: LocalBusiness ×12, FAQPage ×23, BreadcrumbList ×26, Service ×20, Article ×18, BlogPosting ×17 |
| H1 יחיד | 17 מתוך 19 עמודי שורש |
| GA4 + Search Console | `generate_lead` סומן כאירוע מרכזי (08/09) · GSC מחובר מ-25/05 ומפורסם ✅ |

---

## 🔴 ממצאים פתוחים — לפי החזר על מאמץ

### 1. meta description ארוך פי 1.5–2 בכל האתר — **34 מתוך 36 עמודים**
נמדד: 190–281 תווים. גוגל חותך ב-**155–160**.
דוגמאות: `bentonite-drilling` 281 · `projects` 280 · `contractor-license-guide` 278 · `index` 230.
**המשמעות:** כל תיאור באתר נחתך באמצע משפט בתוצאות החיפוש. פוגע ישירות ב-CTR — כלומר בכמות הכניסות, לא רק בדירוג.
**תיקון:** קיצור ל-150–155 תווים, עם ה-CTA בתחילת המשפט ולא בסוף.
**אימות:** `for f in *.html blog/*.html; do ...; done` — אפס עמודים מעל 160.

### 2. `lastmod` — כל 30 הכתובות עם אותו תאריך
נמדד: `grep -o '<lastmod>[^<]*' sitemap.xml | sort | uniq -c` → **30 × `2026-09-06`**.
⛔ **זו אותה בעיה שדווחה ב-24/08, רק עם תאריך חדש.** הערך נצרב מחדש במקום שנבנה גנרטור. תאריך אחיד ולא-אמיתי על כל האתר הוא אות שלילי — הוא אומר לגוגל "הכל השתנה בבת אחת", כלומר שום דבר לא באמת השתנה.
**תיקון:** `scripts/gen-sitemap.mjs` שמריץ `git log -1 --format=%cI -- <file>` לכל עמוד.
**אימות:** אחרי ההרצה — `uniq -c` חייב להחזיר **יותר משורה אחת**.

### 3. `title` ארוך מדי ב-8 עמודים
גוגל מציג ~60 תווים בעברית. נמדד: `bentonite-drilling` 123 · `index` 120 · `blog` 119 · `drainage-pits` 117 · `equipment-rental` 116 · `drainage-pit-home` 109 · `contact` 109 · `earthworks` 96.
**המשמעות:** בכולם הסיומת `| א.א. קידוחים` נחתכת — כלומר **שם העסק לא מופיע בתוצאה**.
**תיקון:** לקצר את החלק התיאורי; להשאיר את שם העסק.

### 4. אין קובץ לוגו בריפו
`find images -iname '*logo*'` → **אפס תוצאות**.
בסכמה: `"logo": "https://eliavafar.co.il/images/gallery/hero-new-1.webp"` — זו **תמונת קידוח, לא לוגו**.
**המשמעות:** גוגל לא יציג לוגו בפאנל הידע ובתוצאות. דורש קובץ ריבועי ≥112px על רקע נקי.
**חוסם:** אין נכס כזה — החלטה של הבעלים.

### 5. 143 קישורים פנימיים עם סיומת `.html`
נמדד: 51× `../index.html#gallery` · 39× `index.html#gallery` · 17× `../index.html#areas` · 11× `index.html#areas` · 6× `index.html#faq` · 19 קישורי בלוג.
כל אחד מחזיר **301** — עובד, אך מבזבז קפיצה בכל קליק ומדלל אות קישור פנימי.

### 6. `review.html` בלי `og:image`
זה הדף שנשלח ללקוחות לבקשת ביקורת. **בוואטסאפ הוא יופיע בלי תמונה.** (`review-qr` ו-`demolition` גם חסרים — אך הם להדפסה/הפניה, לא רלוונטי.)

### 7. `business-card.html` בלי H1
`noindex`, לכן לא משפיע על SEO — אך פוגע בנגישות (קורא מסך).

---

## סדר ביצוע מומלץ
**1** (34 עמודים, השפעה ישירה על כניסות) → **3** (8 עמודים) → **2** (גנרטור) → **5** (חיפוש-והחלפה) → **6** → **7** → **4** (חוסם על נכס).
