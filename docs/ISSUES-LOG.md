# לוג בעיות ותיקונים - אתר א.א. עבודות קידוחים ופיתוח

> מערכת מעקב בעיות, סיבות שורש, פתרונות ומניעה לעתיד.
> כל בעיה מתועדת כדי שנלמד ממנה ונמנע חזרה עליה.

---

## סטטיסטיקה

| סה"כ בעיות | נפתרו | פתוחות |
|------------|--------|--------|
| 7          | 7      | 0      |

---

## ISS-011: רשימות `.service-features` נשברו ב-RTL (רווח ענק אחרי התווית המודגשת)

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-07-17 (אליאב, צילום מסך של דף `drainage-pit-home`) |
| **תאריך תיקון** | 2026-07-17 |
| **חומרה** | בינונית (ויזואלי, פוגע באמינות) |
| **קטגוריה** | UI / RTL / CSS |
| **קבצים מושפעים** | `css/style.css` (`.service-features li`), כל דפי השירות |
| **commit שתיקן** | ראה למטה |

### תיאור הבעיה
בפריט `<li><svg>…</svg><strong>תווית</strong> — טקסט</li>`, ה-`li` הוגדר `display:flex; gap:12px`. ב-flex, ה-`<strong>` והטקסט שאחריו הם **שני פריטי-flex נפרדים** (הטקסט הופך ל-anonymous flex item). כשהתווית ארוכה, הטקסט "נזרק" לשורה נפרדת ונוצר **רווח לבן ענק** אחרי התווית המודגשת. הופיע חזק ב-RTL עם תוויות ארוכות.

### סיבת שורש
`display:flex` על ה-`li` אינו מתאים לתוכן inline מעורב (אלמנט מודגש + טקסט חופשי). flex מפצל כל רצף לפריט נפרד. הבאג היה **סמוי בכל דפי השירות** — התגלה רק כשהתוכן היה ארוך מספיק כדי לגלוש.

⚠️ **לקח ספציפיות (חשוב!):** בניסיון הראשון תיקנתי את `.service-features li` — אבל הרשימות יושבות בתוך `.service-detail-card`, ויש כלל **ספציפי יותר** `.service-detail-card ul li{display:flex}` (0,1,2 מול 0,1,1) שדרס אותו. התיקון "עבד" רק על רשימה שהיתה עטופה ב-`<span>` (שם flex לא מזיק כי הכל פריט אחד), ולכן חשבתי שהוא תקין — עד שהמשתמש הצביע על רשימה לא-עטופה. **מוסר: כשתיקון CSS "לא נתפס", לחפש סלקטור ספציפי יותר עם `getComputedStyle` + לאמת ויזואלית את *כל* המופעים, לא רק אחד.**

### הפתרון
שינוי הכלל הנכון `.service-detail-card ul li` (וגם `.service-features li`) מ-`display:flex` ל-`display:block` עם אייקון ב-`position:absolute; inset-inline-start:0; top:13px` ו-`padding-inline-start:32px`. כך הטקסט זורם כתוכן inline רגיל **ללא תלות באורך או בעטיפת span**. מתקן את **כל** דפי השירות. אומת ויזואלית: dph (רשימה ממוספרת) + drainage-pits (ללא רגרסיה), מובייל 390.

### 🛡️ מניעה (GUARD)
1. **לעולם לא `display:flex` על אלמנט שמכיל אלמנט-inline + טקסט-אח** (strong/a/b ואז טקסט). להשתמש ב-block+absolute-icon או לעטוף הכל ב-`<span>` יחיד.
2. **חובה אימות ויזואלי (screenshot דסקטופ+מובייל) לכל דף/רכיב חדש לפני "מוכן"** — הבאג הזה נוצר כי דילגתי על הצילום. זה מעוגן ב-[[eliavafar_site_health_protocol]] ובפרוטוקול העבודה.
3. תבנית `<strong>תווית</strong> — טקסט` בתוך flex = דגל אדום. לחפש `grep "display: flex" ליד רשימות תוכן.

---

## ISS-001: סוג ערך שגוי ב-aggregateRating (Schema.org)

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-02-19 (Google Search Console) |
| **תאריך תיקון** | 2026-02-22 |
| **חומרה** | קריטית |
| **קטגוריה** | SEO / נתונים מובנים |
| **קבצים מושפעים** | `index.html` |
| **commit שיצר את הבעיה** | `ef10a36` (2026-02-16) |
| **commit שתיקן** | `a86bee0` (2026-02-22) |

### תיאור הבעיה
ערכי `aggregateRating` ב-JSON-LD הוגדרו כמחרוזות (string) במקום מספרים (number):
```json
// שגוי:
"ratingValue": "5",
"bestRating": "5",
"ratingCount": "6",
"reviewCount": "6"
```

### סיבת שורש
טעות בכתיבת JSON-LD - הוספת מרכאות סביב ערכים מספריים. ב-JSON יש הבדל בין `"5"` (string) ל-`5` (number). Google Schema Validator דורש Number עבור שדות מספריים.

### פתרון
הסרת המרכאות מהערכים המספריים:
```json
// תקין:
"ratingValue": 5,
"bestRating": 5,
"ratingCount": 6,
"reviewCount": 6
```

### מניעה לעתיד
- **כלל**: ערכים מספריים ב-JSON-LD (rating, count, price) חייבים להיות ללא מרכאות
- **בדיקה**: לפני push של שינויי Schema.org - להריץ בדיקה ב-[Rich Results Test](https://search.google.com/test/rich-results)
- **תבנית**: להשתמש בתבנית הנכונה מ-schema.org/AggregateRating

---

## ISS-002: פורמט טלפון לא אחיד בין דפים

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-02-22 |
| **תאריך תיקון** | 2026-02-22 |
| **חומרה** | בינונית |
| **קטגוריה** | SEO / נתונים מובנים |
| **קבצים מושפעים** | `index.html`, `contact.html` |
| **commit שתיקן** | `a86bee0` (2026-02-22) |

### תיאור הבעיה
פורמט טלפון שונה בין הדפים:
- `index.html`: `"+972-52-955-6123"` (מקף מיותר)
- `contact.html`: `"052-955-6123"` (חסר קידומת בינלאומית)
- שאר הדפים: `"+972-52-9556123"` (תקין)

### סיבת שורש
כל דף נכתב/עודכן בזמן שונה, ללא תבנית אחידה לפורמט טלפון. אין מקור אמת יחיד (single source of truth) לנתוני העסק.

### פתרון
אחדנו את כל הדפים לפורמט: `+972-52-9556123`

### מניעה לעתיד
- **כלל**: פורמט טלפון תמיד `+972-XX-XXXXXXX` (עם קידומת בינלאומית, מקף אחד אחרי קידומת אזור)
- **בדיקה**: בעדכון כל דף - לחפש `telephone` ולוודא פורמט אחיד
- **שיפור עתידי**: לשקול מעבר ל-template system שמנהל נתוני עסק ממקום אחד

---

## ISS-003: PostalAddress חסרה פרטים ב-contact.html

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-02-22 |
| **תאריך תיקון** | 2026-02-22 |
| **חומרה** | בינונית |
| **קטגוריה** | SEO / נתונים מובנים |
| **קבצים מושפעים** | `contact.html` |
| **commit שתיקן** | (commit של תיקון נוכחי) |

### תיאור הבעיה
ב-`contact.html` ה-PostalAddress הכיל רק `addressCountry: IL` בלי streetAddress, addressLocality ו-postalCode - בעוד ב-`index.html` הפרטים המלאים קיימים.

### סיבת שורש
הדף נוצר בנפרד מדף הבית, ללא העתקת כל פרטי הכתובת. חוסר עקביות בין הדפים.

### פתרון
הוספת הפרטים החסרים בהתאמה ל-index.html:
```json
"address": {
  "@type": "PostalAddress",
  "streetAddress": "אליכין",
  "addressLocality": "אליכין",
  "postalCode": "262",
  "addressCountry": "IL"
}
```

### מניעה לעתיד
- **כלל**: כל LocalBusiness schema חייב לכלול כתובת מלאה
- **בדיקה**: בעת הוספת schema חדש - להשוות מול index.html (הדף המרכזי)
- **צ'קליסט**: name, telephone, email, url, address (מלאה), areaServed

---

## ISS-004: הפניה ב-demolition.html לקובץ במקום URL נקי

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-02-22 |
| **תאריך תיקון** | 2026-02-22 |
| **חומרה** | נמוכה |
| **קטגוריה** | ניתוב / SEO |
| **קבצים מושפעים** | `demolition.html` |
| **commit שתיקן** | (commit של תיקון נוכחי) |

### תיאור הבעיה
דף ההפניה `demolition.html` הצביע על `earthworks.html` (שם קובץ) במקום `/earthworks` (URL נקי שמנוהל ע"י Vercel rewrites).

### סיבת שורש
בעת יצירת דף ההפניה השתמשנו בנתיב קובץ ישיר במקום URL נקי. לא התחשבנו במערכת ה-rewrites של Vercel.

### פתרון
שינוי כל ההפניות ל-URL נקי:
```html
<!-- לפני -->
<meta http-equiv="refresh" content="0;url=earthworks.html">
<!-- אחרי -->
<meta http-equiv="refresh" content="0;url=/earthworks">
```

### מניעה לעתיד
- **כלל**: תמיד להשתמש ב-URLs נקיים (`/earthworks`) ולא בשמות קבצים (`earthworks.html`)
- **הגדרה**: ה-rewrites ב-vercel.json מטפלים בהמרה

---

## ISS-005: חסר datePublished ב-Article schema

| שדה | פרטים |
|-----|-------|
| **תאריך גילוי** | 2026-02-22 |
| **תאריך תיקון** | 2026-02-22 |
| **חומרה** | בינונית |
| **קטגוריה** | SEO / נתונים מובנים |
| **קבצים מושפעים** | `bentonite-drilling.html` |
| **commit שתיקן** | `a86bee0` (2026-02-22) |

### תיאור הבעיה
ה-Article schema ב-`bentonite-drilling.html` חסר את השדות `datePublished` ו-`dateModified` שנדרשים ע"י Google.

### סיבת שורש
בעת יצירת ה-Article schema, לא נכללו שדות תאריך שהם recommended ע"י Google.

### פתרון
הוספת שדות תאריך:
```json
"datePublished": "2025-01-01",
"dateModified": "2026-02-22"
```

### מניעה לעתיד
- **כלל**: כל Article schema חייב לכלול `datePublished` ו-`dateModified`
- **צ'קליסט Article**: headline, author, publisher, datePublished, dateModified, description

---

## ISS-006 — רגרסיית CLS מ-critical-CSS (07/06/2026)

### תיאור
שינוי "critical-CSS" (הטמעת CSS קריטי inline + טעינת ה-stylesheet אסינכרונית) שיפר LCP, אבל גרם ל-CLS לקפוץ מ-0.006 ל-~1.0 (קפיצות עיצוב חמורות) ו-Performance צנח 90→60. נתפס רק בדוח השבועי, ימים אחרי הדחיפה.

### סיבת שורש
השינוי נבדק לפני המיזוג על LCP/FCP/TBT בלבד — **CLS לא נבדק**. ה-swap האסינכרוני של ה-stylesheet הזיז את הפריסה כשה-CSS המלא נטען.

### פתרון
ביטול ה-critical-CSS (commit `756d3a2`) — חזרה ל-CSS חוסם רגיל. CLS חזר ל-0.013, Performance ל-95.

### מניעה לעתיד
- `check-site.mjs` (בדיקה יומית) בודק עכשיו **גם CLS** — median של 3 דגימות, מתריע אם CLS>0.25. רגרסיה כזו תיתפס תוך 24 שעות אוטומטית.
- **כלל**: כל שינוי ביצועים נמדד על **כל** ה-Core Web Vitals (LCP + CLS + TBT), לא רק על המדד שמנסים לשפר.

---

## כללים ולקחים כלליים

### צ'קליסט לפני Push של שינויי ביצועים/CSS:
1. [ ] למדוד **LCP + CLS + TBT** (לא רק את המדד שמשפרים) — A/B ישן מול חדש
2. [ ] שינוי בטעינת CSS (async/critical/inline) → לבדוק CLS חובה (סיכון FOUC/reflow)
3. [ ] לאמת על preview/local לפני מיזוג, לא ישר על production


### צ'קליסט לפני Push של שינויי Schema.org:
1. [ ] ערכים מספריים בלי מרכאות (number, לא string)
2. [ ] טלפון בפורמט `+972-XX-XXXXXXX`
3. [ ] כתובת מלאה (street, locality, postalCode, country)
4. [ ] URLs נקיים (לא שמות קבצים)
5. [ ] Article כולל datePublished/dateModified
6. [ ] עקביות נתונים בין כל הדפים
7. [ ] בדיקה ב-Rich Results Test לפני push

### מקור אמת לנתוני עסק:
- **שם**: א.א. עבודות קידוחים ופיתוח
- **טלפון**: +972-52-9556123
- **אימייל**: eliav1334@gmail.com
- **אתר**: https://eliavafar.co.il
- **כתובת**: אליכין, מיקוד 262, ישראל (IL)
- **קואורדינטות**: 32.2, 34.9
- **טווח מחירים**: $$

---

## ISS-007 — המחשבון לא היה בדפים שמביאים את התנועה (13/07/2026)

### תיאור
מחשבון מחיר בור החלחול נבנה (05/07) ונפרס בדף השירות `drainage-pits` בלבד. דוח Search Console של יוני חשף ש-**76% מהקליקים (72 מתוך 95) מגיעים מ-3 מאמרי מחיר בבלוג** — ו-`drainage-pits-pricing` לבדו מביא 30 קליקים, מדורג על "קידוח בור חלחול מחיר". כלומר: המבקרים הכי חמים באתר — אלה שחיפשו בגוגל בדיוק את השאלה שהמחשבון עונה עליה — מעולם לא ראו אותו.

### סיבת שורש
המחשבון שובץ לפי **מבנה האתר** (דף השירות הוא ה"בית" הטבעי של הנושא) ולא לפי **נתוני התנועה בפועל**. אף אחד לא הצליב את מיקום הפיצ'ר עם הדוח של Search Console. הבלוג נתפס כתוכן SEO משני, בעוד שבפועל הוא שער הכניסה הראשי.

### פתרון (commit `0b9b340`)
- `blog/drainage-pits-pricing` — המחשבון **מוטמע** מיד אחרי טבלת המחירים, במקום CTA גנרי של "צרו קשר". זה הרגע שבו הקורא סיים לקרוא מחירים כלליים ושואל "כמה זה יעלה **לי**".
- `blog/drainage-pits-guide` — קישור בולט מסעיף העלויות (מזין גם קישורים פנימיים לעמוד העצמאי כדי שידורג).
- **הגנת פופאפ:** שני הפופאפים (טיימר 45ש', גלילת 40%) היו נוחתים בדיוק על המחשבון — 40% גלילה זה בדיוק המיקום שלו עכשיו. פופאפ שמבקש טלפון ומסתיר את האומדן מתחרה בעצמו. המחשבון שולח `pcEngaged` ב-postMessage בלחיצה הראשונה; הפופאפים נדחים כל עוד הוא על המסך, ומושתקים לגמרי ברגע שהמבקר התחיל לחשב.

### מניעה לעתיד
- **לפני שמחליטים איפה לשבץ פיצ'ר שמייצר לידים — לפתוח את דוח Search Console ולשבץ אותו בדפים שמביאים את הקליקים, לא רק בדף ה"נכון" לפי מבנה האתר.**
- כל פופאפ/אוברליי חדש חייב להיבדק מול הרכיבים האינטראקטיביים בעמוד. שתי מלכודות לידים שנפתחות באותו רגע = אפס לידים.
- אומת בדפדפן אמיתי על פרוד: האומדן מוצג (₪7,000–12,000), אין פופאפ מעל המחשבון, והפופאפ עדיין קופץ כרגיל בדפים בלי מחשבון.

---

## ISS-008 — ביקורת מקיפה: אבטחה, ביצועים, SEO (13/07/2026)

### תיאור
סריקה כללית של האתר בארבעה צירים. אותרו 3 פערי אבטחה אמיתיים, 4 חסמי ביצועים, ותקלה שקטה שבה לידים לא נכנסו לרשימת התפוצה.

### ממצאי אבטחה + פתרון (commit `1b30bd7`)
1. **אין הגבלת קצב על ה-endpoints הציבוריים.** אומת חי: 5 בקשות POST רצופות ל-`/api/notify-lead` — כולן 200, אפס חסימה. כל בקשה שולחת מייל אמיתי דרך Brevo ⇒ ה-endpoint היה **דיוור חינם** לתוקף: אפשר לשרוף את מכסת Brevo ולהטביע לידים אמיתיים בזבל. נוסף `lib/rate-limit.js` — 5 בקשות/10 דק' לכל כתובת IP + תקרת 40 למופע, לפני כל קריאה ל-Brevo. אומת בפרוד: בקשה 6 → 429.
2. **הזרקת קוד למייל ההתראה שלנו עצמנו.** `phone` נוקה מרווחים/מקפים אך **לא** נמלט, והושתל ישירות לתוך `href="..."`. טלפון מעוצב יכול היה לסגור את התכונה ולהזריק event handler למייל שנשלח לבעלים. תוקן בשורש: מספר טלפון = ספרות, אז מסננים הכל חוץ מספרות לפני שהוא מגיע ל-href.
3. **שגיאות upstream מילוליות.** שני ה-endpoints החזירו את הודעת השגיאה הגולמית של Brevo ללקוח (יכולה לחשוף מצב מפתח/מכסה). עכשיו נרשם בצד שרת, 502 גנרי ללקוח.
4. postMessage ללא בדיקת origin — נוספה בדיקה. לא היה נצול בפועל (X-Frame-Options חוסם מסגור חוצה-מקור, וכל 78 הקישורים עם `target=_blank` נושאים `rel=noopener`), אבל מאזין לא אמור לתת אמון במקור לא-מאומת.

### 🔴 תקלה שקטה — לידים לא נכנסו לרשימת התפוצה (אותרה תוך כדי)
`api/subscribe.js` שלח ל-Brevo בקשה **בלי שום מזהה** כשלא היה אימייל — רק `attributes.PHONE`. Brevo דורש `email` או `ext_id`. מאחר שהאימייל הפך ללא-חובה בטופס (שינוי CRO מיוני), **רוב הלידים מגיעים עם טלפון בלבד ⇒ אף אחד מהם לא נוסף לרשימת אנשי הקשר.** הליד עצמו כן הגיע לבעלים (נתיב נפרד, `notify-lead`) אז לא אבדו פניות — אבל רשימת התפוצה לא נבנתה חודשים. תוקן: `ext_id` = הטלפון בפורמט בינלאומי + attribute `SMS`. אומת בפרוד: טלפון-בלבד מחזיר עכשיו 200 (קודם נכשל).

### ממצאי ביצועים + פתרון (commit `2b8fd28`)
מדידה אמיתית (Slow 4G + האטת CPU פי 4) גילתה ש-**ה-LCP הוא אלמנט טקסט בכל סוגי הדפים**, ו-96% ממנו הוא עיכוב-רינדור ולא הורדה (TTFB כבר 62ms). כלומר הבעיה היא מה שחוסם את הציור, לא מה שמשרתים.
1. `accessibility.css` היה חוסם-רינדור ב-29 דפים והחוליה האיטית ביותר בשרשרת הקריטית (1215ms מול 636ms של `style.min.css`). **כל** כלל בו הוא `position:fixed` (ווידג'ט צף שלא בזרימת המסמך) ⇒ דחייתו לא יכולה להזיז פריסה. נדחה בתבנית `media=print/onload` שכבר בשימוש כאן לפונטים. **⚠️ זה ההפך מ-critical-CSS שגרם ל-ISS-006 — כלום לא מוטמע inline. CLS נשאר 0.**
2. `accessibility.js` נטען **בלי `defer` ב-27 מתוך 29 דפים** (רק index.html ועוד אחד היו תקינים). נוסף `defer` + `fetchpriority=low`.
3. 4 דפי שירות עשו `preload` לתמונת גלריה ב-`fetchpriority="high"` שהיא **לא** אלמנט ה-LCP ומסומנת `loading=lazy` בהמשך הדף. ב-`earthworks.html` התמונה שנטענה בעדיפות גבוהה **לא מופיעה בדף בכלל**. 150-260KB של רוחב-פס בעדיפות עליונה, לפני ה-CSS שחוסם את הציור. הוסרו.
4. קאש CSS/JS משעה → שנה immutable.

### מניעה לעתיד — הכי חשוב
**⚠️ הקאש הארוך היה בטוח רק אחרי שתיקנו את הסיבה שהוא לא היה:** `style.min.css` ו-`main.min.js` נשאו חתימת `?v=` שנכתבה ביד, אבל `accessibility.css` ו-`accessibility.js` **לא נשאו שום חתימה**. קאש של שנה היה נועל גולשים חוזרים על ווידג'ט נגישות ישן לנצח. נוסף `scripts/stamp-assets.mjs` (`npm run stamp`) שחותם את כל ארבעת הקבצים בגיבוב-תוכן — ה-URL משתנה אם ורק אם הבייטים משתנים.
**⇒ אחרי כל שינוי ב-`css/` או `js/` — להריץ `npm run stamp` לפני קומיט.**

### אומת
בדפדפן אמיתי: ווידג'ט הנגישות נטען, מקבל `position:fixed` (הוכחה שה-CSS הנדחה באמת מוחל), הפאנל נפתח, קישור "דלג לתוכן" קיים. `check-site` — 28 דפים תקינים.

### תוספת ל-ISS-008 — הגנת ספאם: מה באמת נחוץ (13/07, אחרי התייעצות)
בדוח הראשון כתבתי "אם יתקפו ברצינות נצטרך שירות חיצוני". **זה היה לא מדויק.** התייעצתי עם מבקר שני (Codex) + מחקר שוק מול התיעוד הנוכחי של Vercel/Cloudflare/Upstash. המסקנה המשותפת:

1. **המגביל In-memory הוא הגנה אמיתית אבל חלשה** — ב-Vercel כל מופע מחזיק Map נפרד, אז `5/10 דק'` בפועל הוא `5 × מספר המופעים`. עדיין מוריד רעש זול באתר קטן. **לא "הצגה", אבל לא חומה.**
2. **⭐ הפתרון הנכון קיים ולא בשימוש: Vercel WAF Rate Limiting.** כלול **חינם** גם בתוכנית Hobby (חוק אחד לפרויקט, מיליון בקשות מותרות כלולות). יושב **לפני** הפונקציה ⇒ בקשה חסומה בכלל לא מפעילה אותה ולא שורפת מייל של Brevo. **הפעלה = טוגל בדשבורד, אפס קוד.** ⛔ פעולה של אליאב.
3. **Redis/Upstash = over-engineering** לעסק עם כמה לידים בחודש. מוסיף תלות חיצונית ומצב-כשל חדש כדי לפתור בעיה שכבר מכוסה. **לא לעשות.**
4. **Cloudflare Turnstile** — חינם, ~שעה עבודה. **רק אם** יתחיל ספאם אמיתי. לא עכשיו.
5. **תקרת נזק טבעית:** התוכנית החינמית של Brevo חוסמת ב-300 מיילים ליום. גם הצפה מוצלחת מוגבלת לזה.

**נוסף בקוד (commit `f9a4d3e`):** `checkOrigin()` — חוסם POST חוצה-אתרים. **CORS לא מגן על השרת, הוא מגן על הדפדפן:** POST חוצה-מקור הוא "בקשה פשוטה" ומגיע לפונקציה בלי קשר ל-`Access-Control-Allow-Origin` — הדפדפן רק מסתיר את *התשובה* מהתוקף. בדיקת Origin בצד שרת היא מה שבאמת חוסם. **מכוון: לא חוסם כשאין Origin/Referer בכלל** (תוספי פרטיות מסירים אותם) — ליד אבוד עולה כסף אמיתי, מייל ספאם עולה 10 שניות. גם נוסף honeypot לפופאפ-הטיימר (היה חסר; פופאפ-הגלילה כן היה מוגן).

### ⚠️ גוצ'ה חדשה — Vercel פורסת פונקציות אחרי סטטיים
בדקתי את חסימת ה-Origin מיד אחרי שקובץ JS סטטי הופיע בפרוד — וקיבלתי 200 (נראה ככישלון). בפועל **ה-serverless functions נפרסות מספר שניות אחרי הקבצים הסטטיים.** אחרי 20 שניות: 403 כמצופה.
**⇒ לאמת שינוי ב-`api/` מול ה-endpoint עצמו, לא מול נחיתת קובץ סטטי.**

---

## ISS-009 — קניבליזציה בין מאמרי הכסף + באג גרשיים ב-meta (13/07/2026)

### תיאור
GSC יוני: 6,560 חשיפות, 95 קליקים, **0 דפים במיקום ראשון**. האבחנה: **הכותרות אינן הבעיה** (אורך תקין, מכילות את מילות המפתח). **הדירוג הוא הבעיה** — במיקומים 10-30, CTR של 1-2% הוא נורמלי בלי קשר לאיכות הכותרת.

### סיבת שורש — פיצול כוח הדירוג
זוגות מאמרים (מדריך + מחיר) מתחרים על אותה שאילתה **בלי אף קישור ביניהם, בשום כיוון** (אומת: 0 קישורים בכל 4 הכיוונים). הכותרת של `bentonite-guide` אפילו הכריזה **"מחירון 2026"** — התחרות חזיתית מול `bentonite-drilling-cost`, שהוא הדף ש-GSC מראה שמביא בפועל את הקליקים. גוגל פיצל את האות בין שני דפים במקום לרכז אותו על המנצח.

### פתרון (commit `4e2ad91`)
1. `bentonite-guide` — הכותרת שונתה לכוונת "איך זה עובד" במקום "מחיר". מפסיק להתחרות. + קישור בולט מסעיף המחירים שלו למאמר המחיר.
2. שני הזוגות (בנטונייט, בורות חלחול) מקושרים הדדית בשני הכיוונים.
3. `foundation-piles-guide` — **התוכן כבר ענה "כמה עולות כלונסאות"** (800₪–2,500₪ בגוף המאמר וב-FAQ), אבל הכותרת הסתירה את זה ⇒ הדף היה **בלתי-נראה** לתבנית החיפוש שמביאה 76% מהקליקים. הכותרת שונתה להוביל במחיר.
4. תיאורי ה-meta של מאמרי הכסף כוללים עכשיו את **המחיר עצמו** — נשלף מטבלת המחירים של כל מאמר, לא הומצא.
5. קישורים פנימיים מצביעים לכתובת **הקנונית** (נקייה), לא ל-`.html` — כדי לא לדלל כוח.
6. 3 תיאורים קוצרו מתחת ל-160 תווים.

### 🔴 באג שיצרתי ותפסתי — גרשיים שוברים תגית meta
כתבתי `למ"א` ו-`ס"מ` בתוך `content="…"` ⇒ **הגרש הכפול סוגר את התכונה מוקדם** והתיאור נחתך באמצע, וגוגל מאנדקס חצי משפט + זבל. תוקן לגרשיים עבריים (`״` U+05F4) — שזה גם הכתיב הנכון.
**מניעה:** נוסף `scripts/check-meta.mjs` (`npm run check-meta`) שסורק את כל הדפים ונכשל על תגית שבורה + מתריע על תיאור מעל 160 תווים. **⇒ להריץ אחרי כל שינוי בכותרת/תיאור.**

### ⚠️ גוצ'ה — קאש CDN מסתיר את הדיפלוי
אימות ראשוני בפרוד החזיר את **התוכן הישן** ונראה ככישלון. הדפים נשמרים ב-CDN. **⇒ לאמת HTML בפרוד תמיד עם `?cb=$RANDOM`.** (מצטרף לגוצ'ות הקיימות: פונקציות נפרסות אחרי סטטיים; כותרות Cache-Control נשמרות גם הן.)

### ⏳ פתוח — היטל הטמנה 2026
המאמר `waste-removal-guide` לא נותן את תעריף היטל ההטמנה, למרות ש-"היטל הטמנה 2026" הוא שאילתה עולה. **לא הוכנס מספר — לא מומצא מידע רגולטורי.** מה שאומת: התעריף צמוד-מדד ומתעדכן ב-1 בינואר. הנתון האחרון שאומת הוא **2025**. הפער המסחרי המעניין: פסולת בניין באתר ייעודי ≈ 5.57₪/טון מול פסולת מעורבת ≈ 125₪/טון — פער של פי ~22 שמתגמל מיון. **דורש אימות ידני מול gov.il/he/pages/prices-landfills** (חוסם גישה אוטומטית).

---

## ISS-010 — כל הקבצים הפנימיים היו חשופים לציבור (13/07/2026)

### תיאור
בבקשת אליאב ("תוודא שאין כפילות והכל מאורגן") התגלה ש-**Vercel מגישה את כל ה-repo כאתר סטטי** — כלומר כל קובץ ב-repo היה נגיש בדפדפן. אומת ב-curl, כולם החזירו **200**:
- **`docs/ISSUES-LOG.md`** — יומן התקלות הזה עצמו, כולל התיאור המפורט של פרצות האבטחה של האתר. גם אחרי תיקון, למסור לתוקף את המפה זו לא עסקה טובה.
- `CLAUDE.md`, `docs/GOOGLE-INTEGRATIONS.md`, `docs/google-ads-launch.md` — תיעוד פנימי ואסטרטגיה שיווקית.
- `scripts/*.mjs`, `.github/workflows/*` — כלי פיתוח ו-CI.
- `js/main.js`, `css/style.css` — מקורות לא-מוקטנים שאף דף לא טוען.
- 🔴 **199MB של תמונות מקור גולמיות** תחת `images/חלוקת תמונות באתר/`. **תמונה בודדת יורדת מהאתר החי במשקל 9.9MB.**

**בדיקת סודות:** אין ערכי מפתחות אמיתיים בקבצים החשופים — רק **שמות** משתנים (`PAGESPEED_API_KEY`, `GMAIL_APP_PASSWORD` כהפניות ל-secrets). לא דלף credential.

### סיבת שורש
1. **ההנחה ש-`.gitignore` מספיק.** התמונות הגולמיות **כבר היו מנוהלות ב-git** לפני שנוספה שורת ה-gitignore — **ו-gitignore לא מסיר ניהול מקובץ שכבר מנוהל.** לכן הן המשיכו להיפרס. (סוכן הביצועים אפילו דיווח בטעות "confirmed in .gitignore — not deployed" — **אימות ב-`git ls-files` הפריך זאת.**)
2. **לא היה `.vercelignore` בכלל.** באתר סטטי, כל מה שב-repo = נכס ציבורי. זו לא תקלה ב-Vercel — זו התנהגות ברירת-מחדל שלא נלקחה בחשבון.

### פתרון (commit `9f1c2e4`)
- נוצר **`.vercelignore`**: `docs/`, `CLAUDE.md`, `.claude/`, `scripts/`, `.github/`, `js/main.js`, `css/style.css`, `טלפונים/`, סקריפטי-עזר. הקבצים נשארים ב-git ובדיסק, וזמינים ל-GitHub Actions (שמושכת מה-repo, לא מהדיפלוי).
- **`git rm -r --cached "images/חלוקת תמונות באתר"`** — 63 קבצים / 199MB הוסרו מהניהול. **הקבצים עצמם לא נמחקו — עדיין בדיסק של אליאב.**

### ⚠️ נשאר חשוף מכוון: `lib/rate-limit.js`
`api/notify-lead.js` עושה לו `require()` בזמן ריצה — **הוצאתו מהדיפלוי תשבור את צינור הלידים.** ניסיתי להעביר ל-`api/_lib/` על **ענף נפרד** (כדי לא לסכן פרוד), אבל Vercel לא חשפה preview דרך GitHub ולא ניתן היה לאמת. **החלטה: לא לדחוף שינוי לא-מאומת לצינור הלידים.** ליד שבור = אובדן כסף אמיתי; חשיפת סף-קצב = כמעט אפס (ניתן לגלות אותו אמפירית בלאו הכי, ו-**Vercel WAF** יושב לפניו וההגדרה שלו *לא* ציבורית).

### כפילויות — נבדק, נקי
- **96/96 תמונות בגלריה בשימוש** — אפס תמונות יתומות.
- אין תמונה שמופיעה **פעמיים באותו דף**. יש זוגות זהי-בייטים בשמות שונים (`equipment-rental-1`=`-4`) אבל **בדפים שונים** — בזבוז בייטים, לא באג ויזואלי. לא נגעתי (סיכון > תועלת).

### מניעה לעתיד
**⇒ בכל בדיקת תקינות: לאמת ש-`docs/`, `scripts/`, `CLAUDE.md` מחזירים 404 בפרוד.**
**⇒ `.gitignore` ≠ לא-מנוהל. לאמת עם `git ls-files` — לא להניח.**

---

## ISS-012 — תפריט עליון בקושי נראה בדפים פנימיים (17/07/2026)

### תיאור
אליאב (צילום מסך של `projects.html`): "שים לב למעלה לכותרות בקושי רואים אותם". קישורי התפריט (דף בית / שירותים / אודות / גלריה / בלוג / צור קשר) היו כמעט בלתי-נראים על רקע ה-header הנייבי.

### חקירה (chrome-devtools, getComputedStyle)
- אבחון ראשוני שגוי: הרצת סקריפט מיד אחרי scroll החזירה `scrolled=false` (ארטיפקט של throttle 100ms) — **כמעט הובילה לתיקון של הבעיה הלא-נכונה.** אחרי `dispatchEvent('scroll')` + השהיה של 400ms: `.scrolled` **כן** עובד, ה-header נייבי `rgba(0,40,65,.95)`.
- הבעיה האמיתית: **`navLinkColor: rgb(71,85,105)`** — קישורי התפריט בדפים הפנימיים צבועים `var(--text-secondary)` (#475569, אפור-צפחה) על רקע נייבי = ניגודיות איומה.

### סיבת שורש
דף הבית משתמש ב-`.nav-link` (לבן 0.85). הדפים הפנימיים משתמשים ב-`<nav><a>` פשוט → נתפסים ע"י `nav a { color: var(--text-secondary) }` (שורה 287 ב-style.css), צבע שתוכנן לרקע בהיר אך ה-header כהה.

### פתרון
`nav a` color → `rgba(255,255,255,0.9)`, hover → `#fff`. תיקון גלובלי (כל הדפים הפנימיים). אומת ש-`.mega-item` (פריטי התפריט הנפתח) לא נשבר — הטקסט שלהם ב-`.mega-title`/`.mega-desc` עם צבעים כהים משלהם על פאנל בהיר. רה-מיניפיקציה של style.min.css + `npm run stamp` (28 דפים). אומת ויזואלית דסקטופ + מובייל.

### מניעה
**⇒ ניגודיות טקסט על header: לזכור שהדפים הפנימיים לא חולקים את מחלקת `.nav-link` של דף הבית — כל שינוי header לבדוק גם על דף פנימי.**
**⇒ בדיקת `.scrolled`/scroll ב-devtools: לחכות אחרי dispatchEvent (throttle) לפני קריאת state — אחרת מאבחנים את הבעיה הלא-נכונה.**

### 🆕 נלווה — נוספה גלריית פרויקטים ל-projects.html
29 תמונות שטח → webp (sharp, 107MB→13MB, 2 רזולוציות + lightbox). אליאב סינן ל-**12** (הסיר 17). מספור openLightbox מוספר-מחדש 0..11 אחרי הסינון. אריחים אחידים 4:3 (`.service-gallery--uniform`, scoped כדי לא להשפיע על דף הבית).

---

## ISS-013 — שם העסק בהדר גולש על שורת הניווט במובייל (19/07/2026)

### תיאור
בבדיקת מובייל (390px) של דף חדרה החדש: "א.א. עבודות קידוחים ופיתוח" נשבר לשתי שורות, והשורה השנייה ("ופיתוח") גלשה מתוך ה-header וכיסתה את שורת הניווט (`דף בית / בלוג / בור חלחול בחדרה`).

### חקירה
- **קודם אומת שזה לא באג שהדף החדש יצר** — אותה חפיפה בדיוק ב-`blog/drilling-netanya.html`. באג ותיק בכל הדפים הפנימיים במובייל.
- ❌ **הנחה ראשונה שגויה:** "אין שום כלל מובייל ל-`.logo`". חיפוש ב-min גילה `header .logo{color:#fff!important;font-size:1.1rem}` — כלל כזה **כן** קיים. עצרתי ומדדתי במקום להמשיך לנחש.
- ❌ **אבחון שני שגוי:** אחרי התיקון הצילום נראה זהה. `getComputedStyle` החזיר `fontSize: 24px` ו-`stylesheetHref: ?v=6a1b14cb` — **הדפדפן הגיש HTML מקאש עם hash ישן.** התיקון היה תקין כל הזמן; רק האימות היה מזוהם.

### סיבת שורש
`header` בגובה קבוע `80px`, ו-`.logo` ב-`font-size: 1.5rem/700` בלי override אפקטיבי לרוחב טלפון. הטקסט (25 תווים) לא נכנס ל-~250px הפנויים אחרי ההמבורגר והאייקון ⇒ נשבר לשתי שורות ⇒ גלש החוצה (`overflow: visible`).

### פתרון
בלוק media חדש בסוף `css/style.css`: `≤768px` → `.logo` 1.15rem + `white-space: nowrap` + אייקון 34px; `≤400px` → 1rem + אייקון 30px. רה-מיניפיקציה + `npm run stamp` (29 דפים).
**אומת במדידה, לא בעין בלבד:** `logoHeight: 30` (שורה אחת), `overflowsHeader: false`, `docScrollX: false`. אומת שדף הבית לא נשבר.

### מניעה
**⇒ אלמנט בתוך container בגובה קבוע: לאמת `rect.bottom > container.bottom` במדידה, לא רק בצילום.**
**⇒ ⚠️ אימות ויזואלי מקומי מחייב `?cb=$RANDOM` בדיוק כמו בפרוד — קאש דפדפן הגיש HTML ישן עם `?v=` ישן וגרם ל"התיקון לא עבד" שקרי. תמיד לאמת שה-`stylesheetHref` שנטען הוא ה-hash החדש לפני שמסיקים שתיקון נכשל.**

### 🆕 נלווה — דף חדרה + הוצאת דפי הלוגו מהפרוד
- נוצר `blog/drilling-hadera.html` (1,836 מילים) — מיקוד **בור חלחול בחדרה**, קרבת אליכין כבידול, קרקע חול/כורכר, כרטיסי יישוב נפרדים, 7 שאלות נפוצות מקומיות. + `blog.html` (כרטיס+schema), `vercel.json` (rewrite — בלעדיו ה-canonical מחזיר 404), `sitemap.xml`.
- 10 דפי `logos-*.html` היו מוגשים לציבור בלי שאף דף מקשר אליהם → נוספו ל-`.vercelignore` (**לא נמחקו** — עדיין נדרשים להחלטת הלוגו).

---

## ISS-014 — שלושה באגים ויזואליים במובייל + בדיקה אוטומטית שתמנע חזרה (19/07/2026)

### תיאור
אליאב שלח 3 צילומי מובייל: (1) "למה זה בפונט הזה?" (2) "למה אין רווחים בין המילים?" (3) "כשגוללים עד הסוף האיקון מסתיר". **דרישתו: "תקלות כאלו לא אמורות להיות תוודא שלא יחזור"** ⇒ לא רק תיקון, אלא **שומר אוטומטי**.

### באג 1 — מילים נדבקות
`.why-card p br { display: none !important }` ב-`@media (max-width: 768px)` הסתיר את ה-`<br>` כדי שהטקסט יזרום בכרטיס צר — אבל ב-HTML **אין רווח סביבו**: `לתוצאה<br/>הטובה` ⇒ `לתוצאההטובה`. 6 כרטיסים.
**תוקן:** רווח לפני ה-`<br/>` (נבלע בדסקטופ, מפריד במובייל).

### באג 2 — משקל גופן שקרי
ה-H1 מוגדר `font-weight: 800` על Rubik, אבל ההזמנה מגוגל ביקשה `400;500;600;700` בלבד. **אומת במדידה:** רוחב Rubik-700 ו-Rubik-800 **זהה** (526px) ⇒ הכותרת רונדרה ב-700 בשקט.
**תוקן:** `800` נוסף להזמנת Rubik ב-31 דפים.

### באג 3 — כפתור הנגישות מסתיר את הצהרת הנגישות
`footer { padding-bottom: 80px }` במובייל. מדידה ב-390×844: `acc-toggle-btn` יושב 160-205px מעל תחתית המסך, וסרגל ה-CTA תופס 75px. **הכפתור כיסה את שורת הזכויות ואת הקישור "הצהרת נגישות" עצמו.**
**תוקן:** `padding-bottom: 220px`.

### ❌ טעות שלי שכמעט עלתה בניתוח מיותר
אבחנתי בביטחון ש"הגופנים לא נטענים" (`document.fonts` החזיר 0 loaded) והתכוונתי לאירוח-עצמי של הגופנים. **מדידה חוזרת אחרי `fonts.ready` + 1.2ש' הוכיחה שהם כן נטענים ופועלים** — הקריאה הראשונה נלקחה מוקדם מדי תחת חנק.
במקביל, **קודקס** תפס טעות לוגית: *"LCP של טקסט נרשם כשהטקסט מצויר לראשונה, לא כשהגופן הסופי מגיע"* ⇒ תיקון גופנים לא היה משפר LCP כלל. טרייס מחונק אישר: הנתיב הקריטי הוא HTML→CSS, **הגופנים לא עליו**.

### 🛡️ מניעה — `scripts/check-visual.mjs` (`npm run check-visual`)
בדיקה סטטית (בלי דפדפן, רצה על כל קומיט) עם 3 כללים שנגזרו משלושת הבאגים:
1. `br-no-space` — `<br/>` בלי רווח בתוך סלקטור שה-CSS מסתיר את ה-`br` שלו במובייל
2. `font-weight-not-loaded` — `font-weight` שמוצהר ב-CSS ואף משפחה לא מזמינה מגוגל
3. `footer-under-fixed-ui` — padding תחתון של הפוטר מתחת ל-200px הנדרשים לפינוי האלמנטים הקבועים

⚠️ **הכלל הראשון נכשל בתפיסה בגרסה הראשונה** — הרגקס עצר ב-`</div>` של האייקון לפני שהגיע ל-`<p>`. תוקן לסריקת `<p>` ישירה.
**אומת בשתי הכיוונים:** הוחזרו שלושת הבאגים ⇒ הבדיקה נכשלה עם 3 ממצאים; שוחזרו התיקונים ⇒ עברה נקי.
נוסף `npm run check-all` = meta + visual + site.

### מניעה כללית
**⇒ בדיקה שעוברת היא חסרת ערך עד שהוכח שהיא נכשלת על הבאג שהיא אמורה לתפוס. תמיד להחזיר את הבאג ולוודא כישלון.**
**⇒ `document.fonts` / מדדי ביצועים: לקרוא רק אחרי `await document.fonts.ready` + השהיה. קריאה מוקדמת מחזירה 0 ומובילה לאבחון שגוי.**
**⇒ מדידה לא-מחונקת אינה ניתנת להשוואה לדוח Lighthouse מחונק. לשחזר את התנאים לפני שמסיקים.**

---

## ISS-015 — sharp 0.34 עם 4 חולשות high (libvips) (27/07/2026)

**נמצא ב:** בדיקת תקינות מקיפה 27/07. `npm audit` החזיר 2 ממצאי high: `sharp <0.35.0` יורש חולשות libvips (CVE-2026-33327 / 33328 / 35590 / 35591).

**חשיפה בפועל:** נמוכה — `sharp` הוא כלי המרת-תמונות מקומי (`fix-images.js`), לא רץ בפרוד (האתר סטטי ב-Vercel). אבל הוא מעבד קבצי תמונה שמגיעים מבחוץ, ולכן שווה לסגור.

**תוקן:** שדרוג ל-`sharp@0.35.3`. `npm audit` → 0 חולשות. מבחן עשן (`sharp({create}).webp().toBuffer()`) עבר — 44 bytes.

**כל השאר נבדק ותקין:** check-site 28 דפים + 3 redirects + sitemap/robots/llms/manifest ✓ · check-meta 35 דפים ✓ · TTFB 70ms · HSTS+CSP+X-Frame+Permissions-Policy ✓ · endpoint לידים + Brevo ✓ · robots.txt מתיר את כל בוטי-ה-AI ✓ · 49 סכמות ✓.

**ביצועים — נמדד, לא הונח:** טרייס Chrome מחונק (Slow 4G + CPU ×4, 412×915): **LCP 1009ms · CLS 0.00 · TTFB 62ms**, מתוכם 946ms render delay. Chrome העריך את החיסכון מ-render-blocking וממטמון ב-**0ms** ⇒ אין מנוף טכני זמין. הפער בין זה ל-79/100 בדוח השבועי הוא חניקת ה-simulation של PageSpeed, לא רגרסיה באתר. ⚠️ **CrUX: אין נתוני שדה לדף** — אין מספיק תנועה אמיתית כדי שגוגל תדגום; זה פער תנועה, לא פער קוד.

**מניעה:** `npm audit` נכנס לרשימת "בדוק תקינות" (כבר בפרוטוקול) — ולא לשדרג sharp בלי מבחן עשן, כי 0.34→0.35 היא גרסה שוברת.

---

## ISS-016 — NAP לא עקבי + "262" בשדה מיקוד (27/07/2026)

**נמצא:** 12 דפים מצהירים `PostalAddress`, ואף אחד לא היה זהה לאחר. `streetAddress` הכיל "אליכין" (שם היישוב, לא רחוב), `postalCode` הכיל **"262"** — לא מיקוד חוקי, ו-`addressRegion` קיבל 4 ערכים שונים בין דפים (מרכז / השרון / מרכז והשרון / עמק חפר), כשהוא שימש בטעות כאזור-שירות במקום אזור-הכתובת (ל-`areaServed` יש שדה משלו, והוא קיים בכל 12).

**⚠️ טעות שלי בסבב הראשון:** הנחתי מזיכרון שהכתובת היא "שקד 11, אליכין 3890800" (מה שמופיע בפרופיל GBP) ודחפתי כך. **אליאב תיקן: 262 הוא ת.ד, לא מיקוד** — והוא לא רוצה כתובת רחוב מפורסמת בכלל.

**מצב סופי (אומת חי בפרוד):** `streetAddress: "ת.ד 262"` · `addressLocality: "אליכין"` · `addressRegion: "מחוז חיפה"` · `addressCountry: "IL"` · **בלי `postalCode`** (0 מופעים). 80 בלוקי JSON-LD נבדקו — כולם תקינים.

**מניעה:** ⇒ **פרטי כתובת/ת.ד/מיקוד הם עובדות של הבעלים — לשאול לפני שדוחפים, לא להסיק מצילום GBP בזיכרון.** ספרה בת 3 ספרות בשדה `postalCode` בישראל היא תמיד באג (מיקוד = 7 ספרות).

---

## ISS-017 — קריאת דוחות GBP: שני הדוחות של יולי הם שני פרופילים, לא שני חודשים (10/08/2026)

**נמצא:** אליאב הביא שני דוחות "ביצועי העסק ביולי 2026" עם מספרים שונים (55 מול 44 אינטראקציות) ושאל למה. **שניהם אותו חודש** — הם שני פרופילי ה-GBP המתועדים ב-`GOOGLE-INTEGRATIONS.md` ("עפר" + "קידוחים"). מזוהים חד-משמעית לפי שדה האתר בכל דוח: אחד `http://eliavafar.co.il/`, השני `https://eliavafar.co.il/`, וכתובות שונות (`שקד 11` מול `שקד`).

**יולי 2026, מאוחד:** 99 אינטראקציות · 717 צפיות · **9 שיחות** · 85 בקשות מסלול · **4 ביקורים באתר בלבד**.

**זה בדיוק הפיצול המוקפא** (מיזוג פרופילים — מוקפא לפי אליאב 21/07, "פרוצדורה ארוכה"). **לא נפתח מחדש כפער.**

**ממצא חדש וזול שכן נפרד מהמיזוג:** בפרופיל אחד שדה האתר הוא `http://` ולא `https://`. אומת חי: `http://eliavafar.co.il/` מחזיר **308 → https** — הקישור עובד, אבל זו קפיצת-הפניה מיותרת ואי-עקביות מול הסכמה. תיקון = עריכת שדה אחד בפרופיל, לא נוגע בפרוצדורת המיזוג.

**⚠️ אי-התאמת NAP מודעת:** האתר מצהיר `ת.ד 262 · אליכין · בלי מיקוד` (החלטת בעלים, ISS-016), ה-GBP מציג `שקד 11, אליכין, 3890800`. זו סתירה אמיתית מול גוגל — אבל היא תוצאה של החלטה מפורשת של אליאב לא לפרסם רחוב. מתועד, לא "באג".

**ביצועים 69/100 בדוח השבועי — לא רגרסיה. אומת:** הקומיט היחיד מאז 29/07 הוא `0d7200f` = **14 שורות של קישורים פנימיים, אפס בייטים של assets**. נמדד חי: TTFB 75ms · דף 80KB · תמונת ה-hero למובייל 52KB (`hero-new-1-800.webp`) עם `preload`+`fetchpriority=high`. תואם למסקנת ISS-015: הפער בין המעבדה המחונקת (LCP 6.1s) לשדה הוא חניקת PageSpeed, ו-**CrUX עדיין בלי נתוני שדה** ⇒ פער תנועה.

**מניעה:** ⇒ **דוח GBP בלי שם-הפרופיל נראה כמו רגרסיה חודשית. לפני שמשווים מספרים בין שני דוחות — לזהות לפי שדה האתר/הכתובת אם זה אותו פרופיל.** כל עוד יש שני פרופילים, כל מספר שאליאב מביא הוא חלקי כברירת מחדל.

---

## ISS-018 — כתבה בתשלום ב"חדשות העיצוב BD" (11/08/2026)

**מה זה:** פרסום 900 ש"ח ב-`lp.vp4.me/iz8n`, גיליון 16/08, הפצה ל-5,400 + פייסבוק/אינסטגרם/טיקטוק. ספק: BD דיגיטל, `+972 52-608-9262`.

**⚠️ הקהל — טעות שלי שאליאב תיקן:** התחלתי לייעץ במפרט טכני (קוטר 120 ס"מ, בנטונייט/פולימר) בהנחה שהקהל = קבלנים ואדריכלים. **אליאב: "למי שזה מפורסם אין ידע בנושא."** אומת מול הגיליון: שאר המפרסמים הם רהיטים, גבס, מרפדייה, עבודות עץ, גגות ⇒ **קטלוג עיצוב לבית, לא ביטאון הנדסי.** ⇒ **הניסוח חייב להיות כללי: מה החברה נותנת, אילו שירותים, דוגמאות וסיפורי הצלחה. אסור מפרט טכני.**

**אורך הטקסט — נמדד, לא הונח:** הכתבה שלו **556 מילים** מול הדוגמאות: ts3w 511 · i8ps 586 · 1qzt 353 · **1xbe (שחר צדיק, קידוחים — הכי דומה) 344**. ⇒ **הכתבה שלו הארוכה מכולן. התחושה ש"המלל מועט" שגויה — זה פורמט קבוע והוא בקצה העליון שלו.** לא לבקש עוד טקסט, לבקש שהטקסט יופנה למקום אחר.

**⭐ הממצא הכי חשוב:** `ts3w` **אינו כתבה — הוא הניוזלטר**: דיגסט של 8 עסקים, לכל אחד כותרת + פסקה + "להמשך קריאה". ⇒ **רוב 5,400 הנמענים יראו רק כותרת+טיזר.** כותרות המתחרים במבנה **שם + הבטחה**; שלו = שם בלבד, ובשגיאה. **הטיזר הוא טקסט נפרד שBD כותב — לבקש לאשר גם אותו, לא רק את הכתבה.**

**טעויות — תועדפו:** קריטי (2): שם החברה בכותרת+og:title = "א.א עבודות **קידוח** ופיתוח" במקום "א.א. עבודות **קידוחים** ופיתוח" · `"ז ה בדיוק"`. לשלוח ברשימה (4): נקודתיים אחרי "עבודות עפר ופיתוח" · מקף אחרי "הסיפור של החברה" · הפסיק ב"ציוד, מתקדם" · הקישור לאתר `/index.html` → `/`. זניח: `alt="image"` ב-9 תמונות.

**❌ תיקון לדיווח קודם שלי:** `"עוד רגע"` ו-`"עיצוב הבית - BD דיגיטל"` **מופיעים בכל 4 הדוגמאות** ⇒ חלק מהתבנית של BD, **לא שגיאות בדף שלו**. הוסרו מהרשימה.

**לוגו ובאנר:** הלוגו עצמו טוב (מקדח משולב באות א׳, נקרא בקטן) — לא לגעת. הבאנר: (1) הלוגו על רקע עמוס בלי משטח הפרדה — נבלע בקטן; (2) הרקע מיוצר ב-AI וניכר (תרנים לא מתחברים, ציוד משוכפל); (3) **אין טלפון על הבאנר**; (4) הכיתוב `"קידוחים ופיתוח מקצועיות שמתחילה עמוק בשטח"` — חסרות נקודתיים. `og:image` = לוגו 900×132 ⇒ ייחתך בוואטסאפ, צריך 1200×630.

**מה לבקש להוסיף (מותאם לקהל):** שירותים בשפת בעל-בית · **2 סיפורי הצלחה** (בעיה→מה עשינו→מה יצא) · "מתי פונים אלינו" · פסקה על מה קורה כשעושים לא נכון (שחר צדיק הקדיש לזה חצי כתבה) · כיתוב מתחת לכל תמונה · תמונה של אליאב עם פנים · לפני/אחרי.

**⛔ מחוץ לפורמט — לא לבקש:** טופס לידים / כפתור הצעת-מחיר. **אין כזה באף אחת מ-4 הדוגמאות.** ב-900 ש"ח לא נלחמים על זה.

**חוסם:** צריך מאליאב **2 מקרים אמיתיים** (איפה · מה הייתה הבעיה · מה בוצע · כמה זמן) כדי לנסח. בלעדיהם BD ימלא בתארים.

**מניעה:** ⇒ **לפני ייעוץ תוכן לפרסום — לזהות את הקהל מהגיליון עצמו (מי עוד מפרסם שם), לא מהתיאור של הלקוח.** "5,400 קבלנים ואדריכלים" נשמע B2B הנדסי; רשימת המפרסמים גילתה קטלוג עיצוב לבית.
